import React from 'react';
import './Spinner.css';

const Spinner = () => {
    return (
        <div className="spinner">
            {/* Add your spinner content here */}
        </div>
    );
};

export default Spinner;